baseURL = 'https://blog.enutie.com'
languageCode = 'en'
title = 'Enutie Blog'
enableRobotsTXT = true    # emit /robots.txt from layouts/robots.txt

# No theme — layouts live in /layouts, styling in /assets & /static.
# See docs/blog-json-feed.md for the homepage feed contract.

# Shared tokens + site chrome come in as a Hugo module from the design repo
# (mounted under assets/{css,js}/enutie and layouts/partials/enutie).
# Update with: hugo mod get -u github.com/Enutie/design
[module]
  [[module.imports]]
    path = "github.com/Enutie/design"

[params]
  author = 'Enutie'
  description = 'My creative journey - art, code, and games'
  images = ['og-image.png']   # default og:image for pages without their own

[menu]
  [[menu.main]]
    name = 'Posts'
    url = '/posts/'
    weight = 10

# The five pursuit tags feed the homepage's pursuit cards.
[taxonomies]
  tag = 'tags'

# Emit per-tag JSON at /tags/<tag>/index.json for the mainside feed.
[outputs]
  home = ['HTML', 'RSS']
  term = ['HTML', 'RSS', 'JSON']

[markup]
  [markup.goldmark.renderer]
    unsafe = true            # allow raw HTML in posts (older content uses it)
  [markup.highlight]
    noClasses = false        # emit classes so code follows the mono/token styling
